class Tileset {
    constructor(file, name, tileDimensions, wrapper) {
        this.file = file;
        this.name = name;
        this.tileDimensions = tileDimensions;
        this.wrapper = wrapper;
    }
}